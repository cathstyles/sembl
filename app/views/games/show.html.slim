#container Loading game...

javascript:
  $(document).ready(function() {
    Sembl.game = new Sembl.Game(#{raw(render(template: "api/games/show", formats: :json, object: @game))});
    Sembl.router = new Sembl.GameRouter(Sembl.game);
    Backbone.history.start();
  });
